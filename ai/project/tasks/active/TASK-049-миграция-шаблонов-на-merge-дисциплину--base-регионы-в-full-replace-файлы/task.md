---
id: TASK-049
schema_version: 2
status: active
mode: interactive
zone: "generator-core"
verification_profile: "ts-generator"
checks: [compile, lint, unit]
max_attempts: 3
depends_on: []
---

# TASK-049: миграция шаблонов на merge-дисциплину (`:base`-регионы в full-replace файлы)

> Второй этап ADR-0007 (первый — guard, закрыт TASK-042/043). Поглощает **BUG-013** и
> **BUG-007** — они об одном и том же: шаблоны без маркеров.
> **Это главное улучшение генератора по существу.** См. обоснование ниже.

## Цель

Сделать регенерацию на живом проекте безопасной *по умолчанию*, а не только «громкой».

Сегодня из ~19 файлов сущности merge-регион `:base` имеют только **три типа** —
`*_dao.dart`, `*_repository.dart` (domain-интерфейс), `*_local_data_source.dart`. Остальное
**full-replace**: любой дописанный руками метод исчезает целиком. Guard из TASK-042 теперь
про это предупредит, но выбор у пользователя бинарный — не генерировать либо потерять.

С `:base`-регионом кастом снаружи региона переживает регенерацию автоматически, и конфликт
не возникает вовсе.

**Замер, на котором основана задача** ([weight-migration-probe](../../docs/weight-migration-probe-2026-07-28.md),
уточнён TASK-048): на реальном проекте **44 merge / 227 full-replace из 271 (16% / 83%)**.

**Адрес, полученный из TASK-048 — с чего начинать.** Из 13 файлов weight, где регенерация
уничтожит ручную работу, **ни один не относится к защищённым типам**. Рвётся ровно то, что
`:base` не покрывает, в порядке частоты:

1. `*_state_providers.dart` (4 файла под угрозой)
2. `*_table_extension.dart` / `*_model_extension.dart` / `*_entity_extension.dart` (6)
3. `*_data_providers.dart` (1)
4. `repository_impl.dart` (1)
5. `remote_data_source.dart` (1)

Это и есть приоритет миграции — не абстрактное «по убыванию пользы», а список файлов,
которые уже сегодня ломаются на живом проекте.

## Не-цели

- НЕ вводить per-method markers (отклонено дискуссией #14).
- НЕ делать patch-only постоянным режимом (отклонено там же).
- НЕ решать правку внутри `:oneToManyMethods` (BUG-030) — отдельная тема.
- НЕ менять guard/ledger (TASK-042/043 закрыты, их контракт не трогать).

## Scope

Разрешено:

- шаблоны `G:/Templates/flutter/t115/**` — **основная зона работы** (обычно STOP-gate; здесь это и есть задача)
- `G:/Templates/flutter/simplified/**` — по согласованию, вторым заходом
- `src/features/generation/**` — только если миграция вскроет дефект в merge-логике
- `src/test/**`
- `ai/project/bug-reports/{007,013}-*.md` — закрытие

Запрещено:

- target-проекты (руками не патчить)
- менять стек/архитектуру шаблона (⚠ stack-lock, Discussion #11)
- ломать существующий вывод: файл без кастомов обязан рендериться байт-в-байт как раньше

## Порядок работы (важно)

Миграция **инкрементальная, по файлу**, а не одним махом:

1. Выбрать файл-шаблон, определить границу «машинное / пользовательское».
2. Обернуть машинную часть в `// === generated_start:base ===` / `generated_end:base`.
3. Прогнать `create-project` + `generate-entity` + `verify` на свежем `t<N>`.
4. Убедиться, что вывод для чистого проекта **не изменился** (diff против предыдущего `t<N-1>`).
5. Следующий файл.

Порядок файлов — по списку-адресу из раздела «Цель» выше (получен замером на weight, а не
предположением): `*_state_providers` → `*_extension` → `*_data_providers` →
`repository_impl` → `remote_data_source` → остальные.

## Критерии приёмки

- [ ] Доля merge-файлов среди генерируемых для сущности выросла с 16% (44 из 271 на weight; 3 из 24 на свежей сущности) до ≥60% — привести замер до/после
- [ ] Для чистого проекта вывод генератора **не изменился**: сравнение свежего `t<N>` с эталоном байт-в-байт (кроме добавленных маркерных строк)
- [ ] Кастом, дописанный снаружи `:base` в мигрированном файле, переживает повторный `generate-entity` **без конфликта** — тест на каждый мигрированный тип файла
- [ ] Кастом внутри `:base` по-прежнему даёт conflict (guard не ослаблен)
- [ ] `verify --name t<N>` errors=0 после каждого этапа миграции
- [ ] BUG-007 и BUG-013 закрыты со ссылкой на эту задачу
- [ ] checks compile/lint/unit зелёные; unit-тесты на merge-логику расширены под новые типы файлов

## Заметки по реализации

- Шаблон t115 — **отдельный git-репо** (`devabacus/t115`, есть remote). Коммит через
  inline `-c safe.directory=G:/Templates/flutter/t115`, push отдельно и только по слову владельца.
- `simplified` — git-репо **без remote** + pre-existing dirty state владельца. Не коммитить без указания.
- Граница региона — вопрос дизайна, а не механики: слишком широкий `:base` = кастому негде
  жить; слишком узкий = машинный код снаружи и рассинхрон при эволюции шаблона. При сомнении
  — остановиться и спросить, а не решать молча.
- Полезный ориентир — те 6 файлов, где `:base` уже есть: посмотреть, как проведена граница там.

## Релевантный контекст

- [weight-migration-probe-2026-07-28.md](../../docs/weight-migration-probe-2026-07-28.md) — замеры, ради которых задача заведена
- `ai/project/docs/decisions/adr-0007-*.md` — этот этап назван там явно
- `ai/project/bug-reports/{007,013}-*.md` — поглощаемые баги
- `src/features/generation/generators/generation_service.ts` — merge-логика (`_plan`, регионы)

## План тестирования

На каждый мигрированный файл: unit на `MockFileSystem` (кастом снаружи региона выживает,
кастом внутри даёт conflict) + E2E на свежем `t<N>` с `verify`. Плюс сквозной замер доли
merge-файлов до/после.
