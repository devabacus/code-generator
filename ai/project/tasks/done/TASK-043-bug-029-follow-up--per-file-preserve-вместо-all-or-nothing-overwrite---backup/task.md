---
id: TASK-043
schema_version: 2
status: done
mode: interactive         # interactive | auto
zone: "generator-core"
verification_profile: "ts-generator"
checks: [compile, lint, unit]
max_attempts: 3
depends_on: []
---

# TASK-043: per-file preserve вместо all-or-nothing + backup перед деструктивной записью

> Follow-up к TASK-042 (merged, master `66ac705`). Поднят Adversarial-ревью как **HIGH-2**.
> **Это блокер для первого прогона guard'а на живом проекте (weight).**

## Цель

Дать пользователю возможность решать судьбу конфликтов **по файлу**, а не «всё или ничего»,
и не терять код безвозвратно при подтверждённой перезаписи.

Сегодня `--overwrite-existing` перезаписывает **все** конфликтующие файлы разом. Контракт
TASK-042 (инвариант «в», п.3) допускает после конфликта два действия **по каждому файлу**:
reviewed overwrite ЛИБО preserve — реализована только гранулярность всего прогона.

## Почему это блокирует weight

Первый прогон на существующем проекте даст N конфликтов: часть — файлы с реальным custom-кодом,
часть — legacy-расхождения без ценности. Разделить их нечем: либо не генерировать вовсе, либо
снести все N. Плюс ledger хранит только хеши и восстановить прежний код не может — backup'а нет
(требование зафиксировано в TASK-042, но не реализовано).

## Не-цели

- НЕ вводить интерактивный TUI-режим выбора по каждому файлу в CLI — достаточно списка путей.
- НЕ трогать классификацию preflight (она уже верна) и форму ledger.
- НЕ решать BUG-030 (`:oneToManyMethods` вне guard'а) — отдельная задача.

## Scope

Разрешено:

- `src/features/generation/generators/{generation_service,preflight}.ts`
- `src/adapters/cli/commands/generate_entity.ts` — приём списка путей у флага
- `src/adapters/vscode/commands/create_data_files_by_replacement.ts` — выбор файлов в preview
- `src/test/**`
- **[РАСШИРЕНИЕ SCOPE, владелец 2026-07-28]** `src/features/generation/generators/{relation_patcher,orchestrator_patcher,app_database_generator}.ts` — только в объёме «не трогать preserved-файлы». Основание: Adversarial-ревью показало, что без этого `preserve` не является preserve (см. «Раунд 2»). Полное заведение патчеров под plan/apply (BUG-030) в эту задачу по-прежнему НЕ входит.

Запрещено:

- шаблоны `G:/Templates/flutter/**`
- «adopt existing as generated» по умолчанию (инвариант «в» TASK-042)
- изменение формы stdout-JSON (поле `conflicts` — это TASK-044)

## Критерии приёмки

- [ ] `--overwrite-existing` принимает список путей (перезаписать только их); без аргументов — прежнее поведение «все», но с явным предупреждением в выводе
- [ ] Файлы, не попавшие в список, остаются нетронутыми и **не сеют baseline** в ledger — тест
- [ ] Backup содержимого перед деструктивной перезаписью (куда именно — решить и обосновать в report.md; вариант по умолчанию `.codegen/backup/<timestamp>/`), путь печатается в выводе
- [ ] Backup не попадает в git по умолчанию (проверить `.gitignore` целевого проекта)
- [ ] VS Code preview позволяет снять галочку с отдельного файла
- [ ] При запуске **с** флагом diff всё равно печатается (сейчас показываются только пути)
- [ ] checks compile/lint/unit зелёные, baseline не падает
- [ ] E2E на свежем `t<N>`: три конфликта → перезаписать один → два остались целы, ledger для них не изменился

## План работы

> Декомпозиция teamlead'а (2026-07-28). Текст пунктов executor НЕ меняет — только статусы
> `[ ]` / `[~]` / `[x] [HH:MM]` / `[!]`.

- [x] **1. Тип выбора в ядре.** [12:05] `GenerationOptions.overwriteExisting` из `boolean` → `true | string[]` (или эквивалент). В apply-фазе перезаписываются ТОЛЬКО выбранные конфликты; невыбранные — preserve: файл не трогается И его baseline не сеется в ledger (инвариант «в» TASK-042 — иначе следующий regen молча сотрёт custom-код). Тест на оба следствия.
- [x] **2. CLI-форма флага.** [12:10] `--overwrite-existing` без значения = прежнее «все» + явное предупреждение в выводе, сколько файлов будет перезаписано; со значением = список project-relative путей. Неизвестный путь в списке (нет среди конфликтов) → fail-fast с перечислением доступных, а не тихий пропуск.
- [x] **3. Backup перед деструктивной записью.** [12:05] Копия прежнего содержимого каждого перезаписываемого файла до записи; путь печатается в выводе. Дефолт — `.codegen/backup/<timestamp>/` с сохранением относительной структуры. Проверить, что backup не уезжает в git (при необходимости — строка в `.gitignore` целевого проекта). Обосновать выбор места в report.md.
- [x] **4. Diff при подтверждённом запуске.** [12:10] Сейчас с флагом печатаются только пути — показывать тот же diff, что и при конфликте: пользователь должен видеть, что именно затирает.
- [x] **5. VS Code preview.** [12:12] Множественный выбор файлов (QuickPick `canPickMany`), отмена = ничего не записано. Минимально, без отдельной панели.
- [x] **6. Тесты.** [12:14] Unit на `MockFileSystem` по каждому критерию приёмки. Ключевой: три конфликта → перезаписать один → два файла целы, их записи в ledger не изменились.
- [x] **7. E2E на свежем `t211`.** [12:24] create-project → generate-entity → три ручные правки в разных файлах → выборочная перезапись одного → проверить содержимое остальных двух и ledger. Плюс `codegen verify --name t211 --human` с числами в report.md.
- [x] **8. Документация.** [12:16] Новое поведение флага в `CLAUDE.md` (секция про regen) и `agent_memory.md`; в `report.md` — явная оценка, снимает ли это блокер для первого прогона на weight.

## STOP-gates

> Перед каждой: `[~]` + `⚠ STOP: <op>` в финальном ответе, вернуть управление. Не выполнять.

- [ ] **Изменение формы stdout-JSON** — остаётся под запретом, это отдельная TASK-044. Поле `conflicts` в этой задаче НЕ добавлять.
- [ ] **Правка шаблонов `G:/Templates/flutter/**`** — Scope запрещает.
- [ ] **Новая npm-зависимость** — не нужна; если покажется нужной, сначала спросить.
- [ ] **`git commit` / `push` / PR / merge** — делает владелец.
- [ ] **Удаление тест-проектов `t<N>`** — запрещено (политика; sandbox блокирует намеренно).
- [ ] **Слом обратной совместимости флага** — если решишь, что `--overwrite-existing` без значения должен перестать означать «все», это breaking change для существующих вызовов: остановись и обоснуй, не меняй молча.

## План работы — раунд 2 (по итогам ревью)

> Standard: REQUEST CHANGES. Adversarial: не готово к мержу, блокер с исполняемым пробником.
> Scope расширен владельцем 2026-07-28.

- [x] **R2-1. БЛОКЕР: preserve должен быть настоящим.** [15:12] Патчеры пишут в обход plan/apply и не знают о выборе пользователя, поэтому `relation_patcher` дописывает регион `:oneToManyMethods` в файл, который просили сохранить — **без backupّа и молча**, при том что генератор печатает «файлы не тронуты». Это регрессия против TASK-042 (там тот же сценарий был fail-closed). Прокинуть множество preserved-путей в `relation_patcher`, `orchestrator_patcher`, `app_database_generator` и пропускать такие файлы целиком. Если пропуск оставляет несогласованное состояние (устаревший регион) — это видимое несоответствие, оно предпочтительнее тихой потери; сообщить пользователю строкой в выводе.
- [x] **R2-2. Тест на воспроизведение блокера — СНАЧАЛА красный.** [15:05] Модель с manyToOne FK, отличным от `customerId` (иначе `manyToOneFields` отфильтрует и патчер не запустится) + preserve файла, который является целью патчера. Тест обязан падать на текущем коде и зеленеть после R2-1. Отдельно — preserve **merge**-файла с `:base` (сейчас все три конфликта в тестах имеют ownership `generated`, самый частый случай на живом проекте не покрыт).
- [x] **R2-3. Тихая эскалация флага.** [15:20] Проверено пробником: `--overwrite-existing a.dart --overwrite-existing` → `true`, то есть точечный выбор молча превращается в тотальную перезапись (реалистично для скрипта-обёртки, дописывающего флаг в конец). Голый флаг после непустого списка не должен сбрасывать выбор в «все» — либо сохранять список, либо fail-fast. Тест.
- [x] **R2-4. Подсказка не должна быть разрушительной.** [15:22] `generate_entity.ts` после списка конфликтов печатает готовую копипаст-строку `--overwrite-existing <ВСЕ пути>` — copy-paste возвращает ровно то all-or-nothing, ради устранения которого задача заведена (нашли оба ревьюера). Убрать готовую «все»-команду; пути уже перечислены выше в отчёте.
- [x] **R2-5. VS Code: второй `generate()` без обработки ошибок.** [15:25] Вызов находится внутри `catch`, своего try нет → `UnknownOverwriteSelectionError` улетает сырым исключением («command resulted in an error»). Обернуть, дать человеческое сообщение. Сценарий достижим: QuickPick не показывает diff, пользователь идёт смотреть файлы в редакторе, format-on-save убирает конфликт.
- [x] **R2-6. Тексты привести в соответствие с реальностью.** [15:30] «Эти файлы не тронуты» (`preflight.ts`), «останутся нетронутыми» (`generate_entity.ts`, VS Code), формулировка в `CLAUDE.md` — после R2-1 они станут правдой; если какой-то путь всё же остаётся вне гарантии, текст обязан это называть, а не умалчивать.
- [x] **R2-7. E2E, который реально поднимает патчер.** [15:35] Прежний E2E на t211 использовал сущность с единственным relation-полем `customerId` → патчер выходил на первой строке, ветка «preserve × патчер» не исполнялась ни разу. Нужна сущность с FK на другую сущность. Свежий `t212` + `codegen verify` с числами.
- [x] **R2-8. Честная оценка по weight.** [15:40] В финальном ответе явно: снимает ли работа блокер первого прогона на weight и что остаётся (BUG-030 в части, не покрытой R2-1; TASK-045/046). Без «вроде».

## Журнал исполнения

- [teamlead 2026-07-28, раунд 2] Ревью: Standard REQUEST CHANGES, Adversarial — блокер (preserve не является preserve; пробник показал потерю кода в preserved-файле без backup). Владелец расширил Scope на патчеры. Задачи раунда 2 — в блоке выше.
- [teamlead 2026-07-28] Ветка `feature/TASK-043-per-file-preserve` создана от master `966b518`. Baseline: 418 passing, compile clean, lint 0 errors / 18 pre-existing warnings.
- [executor] Онбординг пройден (CLAUDE.md, AGENTS.md, executor.prompt.md, task.md, TASK-042 report, agent_memory, ADR-0007, код preflight/ledger/generation_service/CLI/VS Code). Ветка подтверждена: `feature/TASK-043-per-file-preserve`.
- [executor] Проверил поведение commander 14.0.3 на `--overwrite-existing [paths]` эмпирически (пробник в scratchpad): без значения → `true` (обратная совместимость сохраняется), со значением → collector-функция, повторы аккумулируются, следующий за голым флагом известный опшн не съедается. Форма флага выбрана по результатам пробника, не по памяти.
- [11:55] TDD: тесты `src/test/generators/per_file_preserve.test.ts` написаны ДО реализации, `npm run compile` показал красное (34 ошибки: нет `preserved`/`backupDir`, нет модуля `backup`, тип `string[]` не лезет в `boolean`). Реализация после.
- [12:05] **Решение (не из task.md): пустой список = fail-closed, а НЕ «перезаписать всё».** `--overwrite-existing ""` (или `"$FILES"` с незаполненной переменной) подтверждает ноль файлов → подтверждения нет → тот же `GenerationConflictError`. Альтернатива («пустой = все») сделала бы из опечатки в shell-скрипте массовую перезапись. Обратная совместимость голого флага не задета: commander отдаёт `true`, а не `[]`.
- [12:05] **Решение: бэкапятся только ПОДТВЕРЖДЁННЫЕ конфликты.** `create` (файла не было), `overwrite-clean` (на диске побайтово прежний машинный вывод), `seed` (записи нет) ничего не разрушают — копировать их значило бы плодить ~250 копий на каждый штатный regen. Обоснование места (`.codegen/backup/`) и вложенного `.codegen/.gitignore` вместо правки корневого `.gitignore` проекта — в докстринге `backup.ts` (корневой `.gitignore` — часть шаблона и территория пользователя; игнорировать весь `.codegen/` нельзя, `ledger.json` обязан быть в git по ADR-0007).
- [12:05] **Защита от `..` в ключе ledger'а** (`sanitizeBackupRelativePath`): при `--feature-path` вне `--workspace` ключ имеет вид `../../…` (известное ограничение, TASK-045), и `path.join(backupRoot, '../../x')` записал бы «копию» поверх живого дерева — механизм страховки сам стал бы разрушителем. Сегменты `..` → `__`.
- [12:10] **Известный размен fail-fast'а на неизвестный путь:** повтор ровно той же команды после успешной перезаписи упадёт (`UnknownOverwriteSelectionError`) — перезаписанные файлы больше не конфликтуют. Это прямое следствие требования п.2 «а не тихий пропуск»; сообщение объясняет ситуацию отдельной формулировкой для случая «конфликтов нет вовсе». Если владельцу нужен идемпотентный повтор для скриптов — это отдельное решение, молча смягчать не стал.
- [12:20] **Найденное ограничение, НЕ чинил (вне scope).** Patchers пишут в обход plan/apply, поэтому `relation_patcher` может тронуть файл, который пользователь выбрал сохранить: после apply он перезаписывает регион `:oneToManyMethods` в трёх merge-файлах (`*_dao`, `*_local_data_source`, `*_repository`). Custom-код вне этого региона и правка в `:base` при preserve выживают, ledger сохранённому файлу по-прежнему не сеется — то есть новой дыры не появилось, это ровно известный контур [BUG-030](../../bug-reports/030-relation-patcher-otm-region-outside-guard.md). Guard «не патчить preserved-файлы» требует правки `relation_patcher.ts`, а он **не входит в Scope** задачи (и не-цель прямо запрещает решать BUG-030) → оставлено владельцу как вход в TASK-046.
- [12:12] **VS Code:** пустой выбор (`ок` без единой галочки) приравнен к отмене — прежний контракт команды «отмена = ни один файл не изменён»; ни одна галочка не стоит по умолчанию (безопасное значение по умолчанию — сохранить, а не затереть).

- [12:24] **E2E на t211 (свежий, create-project exit 0, 253351ms).** Сценарий полностью пройден: 3 ручные правки в `note_entity.dart` / `note_model.dart` / `note_repository_impl.dart` → regen без флага = exit 1, 3 конфликта, ни одной записи → `--overwrite-existing <только note_model.dart>` = exit 0, перезаписан 1, оставлено 2, backup `20260728-122005-753` содержит ТОЛЬКО перезаписанный файл → на диске ценный код #1 и #3 цел, мусор #2 удалён → sha256 записей ledger'а для двух сохранённых файлов **побайтово те же**, а хеш диска от них отличается (guard взведён) → следующий regen снова даёт ровно 2 конфликта (BUG-029 не отложен на запуск). Fail-fast проверен дважды: опечатка в пути и повтор уже отработавшей команды — оба дают перечень доступных конфликтов и ноль записей. Голый флаг: предупреждение напечатано, 2 файла перезаписаны, затёртый код найден в `.codegen/backup/20260728-122122-655` (восстановимость подтверждена grep'ом).
- [12:24] **Bootstrap-эффект backup'а проверен на t211:** `create-project` (идёт с `overwriteExisting: true`) положил 8 копий скелета `serverpod create`/`flutter create` и создал `.codegen/.gitignore` ДО `git add .` — `git ls-files .codegen` показывает только `.gitignore` и `ledger.json`, каталог `backup/` в индекс не попал. `.codegen/` лежит вне пакетов, поэтому `flutter analyze` копии не видит (подтверждено гейтом ниже).
- [12:25] **Гейты:** compile clean; mocha 440 passing (418 baseline + 22 новых), 0 failing; lint 0 errors / 18 warnings (все pre-existing); `verify --name t211` PASS — errors=0, warnings=1, infos=44 (побайтово совпадает с baseline TASK-042 на t209/t210 → регрессии нет).
- [12:25] **Не проверено runtime:** VS Code QuickPick — GUI-путь, зона владельца (покрыт только компиляцией типов). Остальное покрыто unit + E2E.

### Раунд 2

- [15:05] **R2-2 сделан ПЕРВЫМ и подтверждён красным.** Тест «БЛОКЕР R2-1: preserved-файл — цель relation_patcher'а» написан до единой строчки фикса и упал на текущем коде: `AssertionError: патчер затёр код ВНУТРИ :oneToManyMethods в файле, который просили сохранить` (26 passing, 1 failing). После R2-1 — зелёный. Ловушка учтена: FK ведёт на `folder`, не на `customerId` (иначе `RelationAnalyzer.manyToOneFields` отфильтровал бы и патчер вышел бы на первой строке); предусловие «патчер реально отработал» проверяется assert'ом на `generated_start:oneToManyMethods`. Отдельным тестом закрыт merge-файл с `:base` (`region-modified`) — раунд 1 покрывал только `generated`.
- [15:12] **R2-1: гард `PreservedFiles` (в `preflight.ts`, не новым файлом — Scope).** Держит корень проекта + множество project-relative ключей, метод `blocks(absolutePath)` сам приводит абсолютный путь к ключу ledger'а и **записывает отказ**. Прокинут в `relation_patcher` (проверка сразу после `exists`, до чтения файла — все ветки ниже заканчиваются записью), `orchestrator_patcher` (до `_assertMarkersPresent`: сохранённый файл с битыми маркерами не должен обрушивать прогон, писать в него мы всё равно не собирались) и `AppDatabaseGenerator.generate(preserved)` (вызывается адаптерами ПОСЛЕ `generate()`, поэтому гард отдаётся наружу в `GenerationResult.preservedFiles` — один накопитель на прогон, иначе отчёт показал бы половину правды).
- [15:12] **Решение: пропуск, а не перезапись и не отказ всему прогону.** Пропуск оставляет устаревший машинный регион — это видимое несоответствие, и оно предпочтительнее тихой потери. Отказать всему прогону нельзя: подтверждённые файлы уже записаны фазой apply, откатывать их нечем. Пропуск **печатается** (`formatPatchSkipReport`) — молчаливый отказ писать это тот же обман, что и молчаливая запись, только в другую сторону.
- [15:12] **Честный объём R2-1.** Реальное пересечение целей патчера с планом `generate-entity` даёт только `relation_patcher` (7 шаблонов: `*_dao`, `*_repository`, `*_repository_impl`, `*_usecases`, `*_local_data_source`, `*_local_datasource_service`, `*_usecase_providers`). `sync_orchestrator_provider.dart` и `database.dart` приходят из manifest `startProject`, в план `generate-entity` не входят и конфликтом стать не могут — там гард защитный (покрыт unit-тестом с контрольной группой). В докстрингах это сказано прямо, чтобы следующий агент не считал их проверенными в бою.
- [15:20] **R2-3: смешение форм → отказ, а не угадывание.** Пробник (`scratchpad/probe_escalation.js`, commander 14.0.3) воспроизвёл обе беды: `--overwrite-existing a.dart --overwrite-existing` → `true` (точечный выбор молча становится тотальной перезаписью), обратный порядок → `['a.dart']` (голый флаг молча выброшен). Различить формы можно только в потоке событий `option:overwrite-existing` (`null` = голое вхождение); `parseArg` для голого флага commander не вызывает вовсе. Поэтому `parseArg` у опции снят, поле `overwriteExisting` убрано из `GenerateEntityOptions` (чтобы не было второго источника истины), значение считает чистая `resolveOverwriteFlag(occurrences)`. Смешение → `ConflictingOverwriteFormsError` **до** чтения YAML и любой работы. Проверено на реальном CLI: exit 1, ни одной записи.
- [15:22] **R2-4:** готовая строка `--overwrite-existing <ВСЕ пути>` из ветки конфликта убрана — теперь плейсхолдер `<путь1>,<путь2>` и явное указание брать пути из списка выше. В сообщении `GenerationConflictError` фраза про голый флаг переформулирована из нейтральной в предупреждение.
- [15:25] **R2-5:** второй `generate()` в VS Code обёрнут собственным try/catch с разбором `UnknownOverwriteSelectionError` (набор конфликтов изменился, пока был открыт QuickPick — например, format-on-save убрал конфликт) и `GenerationConflictError` (появились новые). Обе ветки — человеческое сообщение + возврат, вместо сырого «command resulted in an error». `result` поднят в переменную функции: `AppDatabaseGenerator` теперь обязан получить `result.preservedFiles` и на успешном пути тоже.
- [15:35] **R2-7: E2E на свежем t212** (`create-project` exit 0, 257121 ms). Сущность с FK на ДРУГУЮ сущность: `folder` сгенерирована первой, затем `note` с `folderId, relation(parent=folder)` в ту же feature. Патчер подтверждённо отработал — `getNotesByFolderId` появился и в `note_dao.dart`, и в `note_repository_impl.dart` (в раунде 1 на t211 эта ветка не исполнялась ни разу). Три ручные правки: (1) `note_dao.dart` — комментарий внутри `:base` (источник конфликта) + метод `e2eCountNotes()` **внутри `:oneToManyMethods`**; (2) `note_repository_impl.dart` — код **внутри `:oneToManyMethods`**; (3) `note_entity.dart` — мусор. Regen без флага: exit 1, 3 конфликта, ноль записей. Regen с `--overwrite-existing <только note_entity.dart>`: exit 0, перезаписан 1, оставлено 2, **оба сохранённых файла байт-в-байт прежние** (sha `0d3f9455…` и `2dc45be3…` не изменились), ручной код внутри `:oneToManyMethods` на месте в обоих, записи ledger'а для них не изменились, backup содержит ровно один файл. Следующий regen снова даёт ровно 2 конфликта. Хеш перезаписанного `note_entity.dart` совпал с его записью в ledger (файл вернулся к машинному выводу). `git ls-files .codegen` → только `.gitignore` и `ledger.json`; `git check-ignore` подтверждает, что копия скрыта правилом `backup/`.
- [15:40] **Гейты раунда 2:** compile clean; mocha **459 passing** (440 после раунда 1 + 19 новых), 0 failing; lint 0 errors / 18 warnings (все pre-existing); `verify --name t212` **PASS — errors=0, warnings=1, infos=44** (побайтово совпадает с baseline TASK-042 на t209/t210 и с t211 раунда 1 → регрессии нет).
- [15:40] **R2-8, честно по weight.** Блокер первого прогона **снят**: разделить N конфликтов на «перезаписать» и «сохранить» теперь можно, сохранённые файлы не трогает никто (включая `relation_patcher`, который до раунда 2 их переписывал), прежнее содержимое перезаписанных лежит в `.codegen/backup/<timestamp>/`. **Остаётся, и это надо знать до прогона:** (1) **[BUG-030](../../bug-reports/030-relation-patcher-otm-region-outside-guard.md) открыт** — правка внутри `:oneToManyMethods` сама по себе не порождает конфликт (baseline merge-файла хеширует только `:base`), поэтому при штатном regen без конфликта такая правка по-прежнему затирается молча; R2-1 закрыл только случай «файл уже в конфликте и выбран preserve»; (2) **TASK-045** — `--feature-path` вне `--workspace` даёт ключи ledger'а вида `../../…` (в backup обезврежено `sanitizeBackupRelativePath`, в самом ledger'е — нет); (3) **TASK-046** — записи ledger'а для писателей вне plan протухают; (4) **TASK-044** — `conflicts` в stdout-JSON нет, скрипт-обёртка вынуждена парсить stderr; (5) VS Code QuickPick не проверялся runtime (GUI-путь, покрыт только типами) — на weight первый прогон надёжнее делать через CLI.
- [15:35] **Промах в ходе E2E, исправлен:** первая попытка правила `domain/entities/note_entity.dart` — такого файла нет (сущность лежит в `domain/entities/note/note_entity.dart`), `appendFileSync` создал посторонний файл и конфликтов оказалось 2 вместо 3. Файл удалён, правка перенесена на настоящую сущность. Урок: путь целевого файла проверять `ls`, а не выводить из имени.

## Релевантный контекст

- `ai/project/tasks/done/TASK-042-*/report.md` — раздел «Ревью и раунд 2», HIGH-2
- `ai/project/docs/decisions/adr-0007-*.md` — state machine legacy
- `src/features/generation/generators/preflight.ts` — классификация и `GenerationConflictError`

## План тестирования

Unit на `MockFileSystem` по каждому критерию + E2E на свежем `t<N>` (incremental numbering).
